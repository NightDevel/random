
#include <stdio.h>

#ifdef DEBUG
#define dprintf(fmt, ...) do { fprintf(stderr, fmt, __VA_ARGS__); } while (0)
#else
#define dprintf(fmt, ...) 
#endif

#define MAX_FIBS (2000000+10)
int MAX_FIBS
void calculate_fib()
{
    F[0]

}


int main()
{
    int t;
    int N;
    scanf("%d", &t);
    while (t--){
    }
    return 0;
}

